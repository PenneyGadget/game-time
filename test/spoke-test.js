let assert = require('chai').assert;
const Spoke = require('../lib/scripts/spoke');

it('Spoke is a function', function() {
  assert.typeOf(Spoke, 'function');
});

describe('stuff', function() {
  xit('does stuff', function() {

  });
});
